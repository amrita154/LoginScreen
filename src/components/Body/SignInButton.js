import React from 'react';
import {Text,View, Button} from 'react-native';

SignInButton=({value})=>{
    return(
        <Button title={value}>

        </Button>
    )
}
export default SignInButton;